# Scripts

**Purpose**: small utility scripts that save the team from repeating multi-step manual processes.

**Ownership**: whoever writes a script owns explaining it here.

**Contents**:
- `dev.sh` — starts backend and frontend dev servers together.

**Convention**: a script belongs here only if more than one person will run it more than once. One-off exploration scripts don't need to be committed.
