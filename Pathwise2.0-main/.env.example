# Root-level reference only — the real .env files live in each app:
#
#   backend/.env    (copy from backend/.env.example)
#   frontend/.env   (Vite only needs this if you add VITE_-prefixed vars later)
#
# See backend/README.md for the full list of backend environment variables.
